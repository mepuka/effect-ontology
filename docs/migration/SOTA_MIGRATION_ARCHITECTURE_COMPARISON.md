# Architecture Comparison: Current vs. SOTA-Aligned

## Current Architecture (Entity-Centric)

```
┌─────────────────────────────────────────────────────────────────┐
│                         INPUT TEXT                               │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                    TEXT CHUNKING (NLP)                           │
│  • Sentence segmentation                                         │
│  • Sliding window (size=3, overlap=1)                           │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                  PROMPT GENERATION                               │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ Ontology Graph → KnowledgeIndex (HashMap)            │       │
│  │   • Topological catamorphism                         │       │
│  │   • Class hierarchy with properties                  │       │
│  └──────────────────────────────────────────────────────┘       │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ Entity Discovery → EntityCache                       │       │
│  │   • Previously discovered entities                   │       │
│  │   • Cross-chunk entity tracking                      │       │
│  └──────────────────────────────────────────────────────┘       │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ StructuredPrompt                                     │       │
│  │   • System: Class definitions                        │       │
│  │   • Context: Known entities                          │       │
│  └──────────────────────────────────────────────────────┘       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│              LLM EXTRACTION (Single Stage)                       │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ Schema: Entity-based JSON-LD                         │       │
│  │ {                                                     │       │
│  │   entities: [                                         │       │
│  │     {                                                 │       │
│  │       "@id": string,         ← LLM GENERATES IRI     │       │
│  │       "@type": ClassIRI,                             │       │
│  │       properties: [                                  │       │
│  │         {                                             │       │
│  │           predicate: PropertyIRI,                    │       │
│  │           object: string | {"@id": string}           │       │
│  │         }                                             │       │
│  │       ]                                               │       │
│  │     }                                                 │       │
│  │   ]                                                   │       │
│  │ }                                                     │       │
│  └──────────────────────────────────────────────────────┘       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                  JSON → RDF CONVERSION                           │
│  • Entity "@id" → N3.NamedNode or N3.BlankNode                  │
│  • Properties → RDF triples                                      │
│  ⚠️  PROBLEM: Invalid IRIs like "<Stanford University>"          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                 TURTLE SERIALIZATION                             │
│  • N3.Writer creates Turtle string                              │
│  • May contain malformed IRIs                                    │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│            ENTITY RESOLUTION (Merge Chunks)                      │
│  • Parse Turtle back to N3.Store                                 │
│  ⚠️  FAILURE: N3.Parser chokes on "<Stanford"                    │
│  • Label-based deduplication                                     │
│  • Skolemization of blank nodes                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                  FINAL RDF OUTPUT                                │
└─────────────────────────────────────────────────────────────────┘
```

## SOTA-Aligned Architecture (Triple-Centric)

```
┌─────────────────────────────────────────────────────────────────┐
│                         INPUT TEXT                               │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                    TEXT CHUNKING (NLP)                           │
│  • Sentence segmentation                                         │
│  • Sliding window (size=3, overlap=1)                           │
│  [UNCHANGED]                                                     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                  PROMPT GENERATION                               │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ Ontology Graph → KnowledgeIndex (HashMap)            │       │
│  │   [UNCHANGED]                                         │       │
│  └──────────────────────────────────────────────────────┘       │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ Entity Discovery → EntityCache                       │       │
│  │   [UNCHANGED]                                         │       │
│  └──────────────────────────────────────────────────────┘       │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ StructuredPrompt + Entity Consistency Rules  ✨ NEW  │       │
│  │   • System: Class definitions                        │       │
│  │   • System: Entity naming rules                      │       │
│  │   • Context: Known entities                          │       │
│  └──────────────────────────────────────────────────────┘       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│         LLM EXTRACTION (Two-Stage)  ✨ CHANGED                   │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ STAGE 1: Extract Entities                            │       │
│  │ Schema: EntityList                                   │       │
│  │ {                                                     │       │
│  │   entities: [                                         │       │
│  │     {                                                 │       │
│  │       name: "Alice",      ← Human-readable           │       │
│  │       type: "Person"                                 │       │
│  │     },                                                │       │
│  │     {                                                 │       │
│  │       name: "Stanford University",                   │       │
│  │       type: "Organization"                           │       │
│  │     }                                                 │       │
│  │   ]                                                   │       │
│  │ }                                                     │       │
│  └──────────────────────────────────────────────────────┘       │
│                         │                                        │
│                         ↓                                        │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ STAGE 2: Extract Relations (with known entities)     │       │
│  │ Schema: TripleGraph                                  │       │
│  │ {                                                     │       │
│  │   triples: [                                          │       │
│  │     {                                                 │       │
│  │       subject: "Alice",                              │       │
│  │       subject_type: "Person",                        │       │
│  │       predicate: "works_at",                         │       │
│  │       object: {                                       │       │
│  │         value: "Stanford University",                │       │
│  │         type: "Organization"                         │       │
│  │       }                                               │       │
│  │     }                                                 │       │
│  │   ]                                                   │       │
│  │ }                                                     │       │
│  └──────────────────────────────────────────────────────┘       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│              TRIPLE → RDF CONVERSION  ✨ NEW                     │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ 1. Build Entity Registry                             │       │
│  │    "Alice" → :alice                                  │       │
│  │    "Stanford University" → :stanford_university      │       │
│  │                                                       │       │
│  │ 2. Sanitize Names → Valid IRIs                       │       │
│  │    • Replace spaces with underscores                 │       │
│  │    • Remove special characters                       │       │
│  │    • URL encode                                       │       │
│  │                                                       │       │
│  │ 3. Generate RDF Quads                                │       │
│  │    :alice a foaf:Person .                            │       │
│  │    :alice rdfs:label "Alice" .                       │       │
│  │    :alice foaf:works_at :stanford_university .       │       │
│  │    :stanford_university a foaf:Organization .        │       │
│  │    :stanford_university rdfs:label                   │       │
│  │       "Stanford University" .                        │       │
│  └──────────────────────────────────────────────────────┘       │
│  ✓ SOLVED: No malformed IRIs, controlled generation             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                 TURTLE SERIALIZATION                             │
│  • N3.Writer creates valid Turtle                               │
│  • All IRIs properly formatted                                  │
│  [UNCHANGED - but now receives valid input]                     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│            ENTITY RESOLUTION (Merge Chunks)                      │
│  • Parse Turtle back to N3.Store                                 │
│  ✓ SUCCESS: Valid Turtle, no parse errors                       │
│  • Label-based deduplication                                     │
│  • Skolemization of blank nodes                                  │
│  [UNCHANGED - but now receives valid Turtle]                    │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                  FINAL RDF OUTPUT                                │
└─────────────────────────────────────────────────────────────────┘
```

## Key Differences Summary

| Component              | Current                    | SOTA-Aligned             | Impact    |
| ---------------------- | -------------------------- | ------------------------ | --------- |
| **LLM Output Format**  | Entity-centric JSON-LD     | Triple-based SPO         | 🔴 High   |
| **ID Generation**      | LLM generates IRIs         | Human-readable names     | 🔴 High   |
| **Extraction Stages**  | Single stage               | Two stages               | 🟡 Medium |
| **Entity Consistency** | Implicit                   | Explicit rules in prompt | 🟡 Medium |
| **IRI Creation**       | At LLM output              | At RDF conversion        | 🔴 High   |
| **Schema Complexity**  | Nested (entity→properties) | Flat (triples)           | 🟡 Medium |

## Data Structure Comparison

### Current: Entity Schema

```typescript
{
  "@id": "<Stanford University>",    // ⚠️ Invalid IRI
  "@type": "Organization",
  "properties": [
    {
      "predicate": "http://xmlns.com/foaf/0.1/name",
      "object": "Stanford University"
    },
    {
      "predicate": "http://xmlns.com/foaf/0.1/location",
      "object": {
        "@id": "<California>"         // ⚠️ Invalid IRI
      }
    }
  ]
}
```

### SOTA: Triple Schema

```typescript
;[
  {
    subject: "Stanford University",
    subject_type: "Organization",
    predicate: "http://xmlns.com/foaf/0.1/name",
    object: "Stanford University"
  },
  {
    subject: "Stanford University",
    subject_type: "Organization",
    predicate: "http://xmlns.com/foaf/0.1/location",
    object: {
      value: "California",
      type: "Location"
    }
  }
]
```

## RDF Output (Identical in Both Cases)

```turtle
@prefix foaf: <http://xmlns.com/foaf/0.1/> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix : <http://example.org/> .

:stanford_university a foaf:Organization ;
    rdfs:label "Stanford University" ;
    foaf:name "Stanford University" ;
    foaf:location :california .

:california a :Location ;
    rdfs:label "California" .
```

**Critical Insight**: The RDF output is semantically identical. The difference is only in the LLM interface layer, which now generates clean entity names instead of attempting IRI generation.

## Advantages of SOTA Approach

1. ✅ **No IRI Parsing Errors**: Names are sanitized programmatically
2. ✅ **Better Entity Consistency**: Two-stage ensures name reuse
3. ✅ **Simpler LLM Task**: Extracting names vs. generating URIs
4. ✅ **SOTA Alignment**: Matches GraphRAG, KGGen, LangChain patterns
5. ✅ **Backward Compatible**: Can convert between formats
6. ✅ **Maintained Strengths**: Ontology guidance, validation, Effect architecture unchanged
