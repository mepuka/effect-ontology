# =============================================================================
# Test Environment Configuration
# =============================================================================
#
# Used by test scripts (scripts/test-server.sh, scripts/test-extract.sh)
# This file is version-controlled and provides consistent test environment.
#
# Usage:
#   ./scripts/test-server.sh    (uses --env-file=.env.test)
#   ./scripts/test-extract.sh   (sources for paths only)
#
# API Key Management:
#   LLM_API_KEY is NOT stored here (security).
#   Test scripts load it from monorepo .env (VITE_LLM_ANTHROPIC_API_KEY)
#   or you can export it manually before running tests.

# =============================================================================
# LLM Configuration
# =============================================================================
LLM_PROVIDER=anthropic
LLM_MODEL=claude-haiku-4-5
# LLM_API_KEY - loaded by test script from monorepo .env
LLM_TIMEOUT_MS=60000
LLM_MAX_TOKENS=4096
LLM_TEMPERATURE=0.1

# =============================================================================
# Storage Configuration
# =============================================================================
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=/tmp/effect-ontology-test

# =============================================================================
# Ontology Configuration
# =============================================================================
# IMPORTANT: Must be an absolute path for local file-system OntologyService.
# Relative paths fail because they are resolved from CWD, not package root.
# This absolute path points to the football ontology in the monorepo.
ONTOLOGY_PATH=/Users/pooks/Dev/effect-ontology/ontologies/football/ontology.ttl
ONTOLOGY_CACHE_TTL=3600

# =============================================================================
# Runtime Configuration
# =============================================================================
RUNTIME_CONCURRENCY=4
RUNTIME_LLM_CONCURRENCY=2
RUNTIME_RETRY_MAX=3
RUNTIME_RETRY_INITIAL_DELAY=1000
RUNTIME_RETRY_MAX_DELAY=30000
RUNTIME_ENABLE_TRACING=false

# =============================================================================
# Grounder Configuration
# =============================================================================
GROUNDER_ENABLED=true
GROUNDER_THRESHOLD=0.8
GROUNDER_BATCH_SIZE=5

# =============================================================================
# RDF Configuration
# =============================================================================
RDF_BASE_NAMESPACE=http://example.org/kg/
RDF_OUTPUT_FORMAT=Turtle

# =============================================================================
# Server Configuration
# =============================================================================
PORT=8080
NODE_ENV=test
